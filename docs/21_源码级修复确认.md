# 36 源码级验证：2.9.5+ 修复已确认存在于代码（#71 最后证明项）

> 日期：2026-08-19 ｜ 仓库：microsoft/WSL（本地 sparse 克隆 wsl-src-master-sparse，blob:none）
> 关联：34 号文档（2.9.4 运行时验证 + 回滚）、31 号（openvmm 溯源 + 版本缺陷区间勘误）、32 号（探针基线）
> 状态：**源码证据闭环；运行时验证待 Store 推送 2.9.5+（外部阻塞）**

## 一、为什么转源码验证

- 用户确认：#71 是除 #69 外最后一项执行项；2.9.5/2.9.6/2.9.7 **没有安装资产**（GitHub release API 实证：只有源码 zip/tar.gz；可安装资产止于 2.7.12 stable 与 2.9.4 preview）。
- 安装不了 → 运行时验证不可执行 → 转向**源码级验证**：修复 commit 是否真实进入 2.9.5+ 的代码。
- 零副作用：只读 git 操作，不碰本机 WSL 环境。

## 二、验证对象（两个修复 commit）

| PR | 标题 | 合并时间 | merge commit |
| --- | --- | --- | --- |
| microsoft/WSL PR 41051 | fix: port 0 tracking not working for threads | 2026-08-05 | 55d457846d5e5e5d3b82f7c2305480a2aa3893e7 |
| 上游 PR 41125 | Track implicit autobind ports from listen() in Consomme networking | 2026-07-24 | 96b220c785f6ed90e103813cdfc66a723224c657 |

修复文件：src/linux/init/GnsPortTracker.cpp（#41051 +14/-1；#41125 +102/-3）、localhost.cpp、main.cpp、seccomp_defs.h、test/windows/NetworkTests.cpp。

## 三、祖先验证结果（git merge-base --is-ancestor）

| tag | #41051（PIDFD_THREAD） | #41125（listen 隐式 autobind） | 与运行时实测对照 |
| --- | --- | --- | --- |
| 2.7.8 | 无 | 无 | 本机实测 STALL 91.5%（200 轮直方图）✅ 吻合 |
| 2.7.12（最新 stable） | 无 | 无 | 未测（按源码必然复现） |
| 2.9.3 | 无 | 无 | — |
| 2.9.4（最新 preview） | 无 | 无 | 实测 STALL 87.5%（40 轮）✅ 吻合 |
| 2.9.5 | **有** | **有** | 无法安装，未测 |
| 2.9.6 | **有** | **有** | 无法安装，未测 |
| 2.9.7 | **有** | **有** | 无法安装，未测 |

**边界完全对齐**：实测复现的版本（2.7.8.0、2.9.4.0）全部缺修复；2.9.5+ 全部含修复。

## 四、修复代码实际内容（2.9.4 → 2.9.5 diff 实证）

### 4.1 #41051：pidfd_open 增加 PIDFD_THREAD（非主线程 bind 追踪）

```cpp
// GnsPortTracker.cpp: DuplicateSocketFd
// PIDFD_THREAD requires kernel >= 6.9. Fallback to process only if not supported.
int pidFdResult = static_cast<int>(syscall(SYS_pidfd_open, Pid, PIDFD_THREAD));
if (pidFdResult < 0 && errno == EINVAL)
{
    pidFdResult = static_cast<int>(syscall(SYS_pidfd_open, Pid, 0u));
}
```

- 直接对应 31 号文档的根因假设 1：旧版只跟踪进程主线程的 bind，**非主线程 bind 的端口 0 注册丢失**。
- 带 EINVAL 回退（内核 < 6.9），说明作者有意兼容旧内核。

### 4.2 #41125：listen() 隐式 autobind 纳入追踪

```cpp
// GetCallInfo 新增 ParseListen：
// listen() can trigger an implicit autobind (assigning an ephemeral port) on a socket
// that was never explicitly bind()'d ... duplicate the fd and check getsockname()
// immediately: if it's already bound ... resolve the port synchronously here.
// Otherwise defer resolution to ResolvePortZeroBind(), the same as a bind(port=0) call.

// x86_64 路径：__NR_listen → ParseListen(Arguments[0])
// 32bit socketcall 路径：Arguments[0] == SYS_LISTEN → 读内存取 fd → ParseListen
// ARM 路径：__NR_listen || ARMV7_NR_listen → ParseListen(Arguments[0])
```

- 直接对应 31 号文档的根因假设 2：**listen() 不显式 bind 时内核隐式分配临时端口，旧版不拦截 listen → 端口注册丢失**。
- 覆盖 x86_64 / 32 位 socketcall / ARM 三条路径，说明是完整修复而非单点补丁。

### 4.3 版本间稳定性

- `git diff 2.9.5 2.9.7 -- GnsPortTracker.cpp localhost.cpp main.cpp seccomp_defs.h`：**空**（三个版本修复代码完全一致，无回滚无漂移）。
- `git log 2.9.7..master`：仅有 1e14a49（VMP 未安装警告，与端口追踪无关）——修复后无相关回归改动。

## 五、结论

1. **版本缺陷区间实锤（源码级）**：2.7.8.0 → 2.9.4.0 缺两个修复且实测复现；2.9.5+ 含两个修复。
2. 修复代码内容与 31 号文档根因分析**逐条对应**（PIDFD_THREAD / listen autobind），不是"可能相关"，是"精确修复"。
3. 对外叙事升级：引用 merge commit SHA（55d45784 / 96b220c7）+ 祖先验证表，替代"GitHub compare 显示已合并"的弱证据。

## 六、剩余缺口（诚实声明）

- **运行时验证仍未做**：#71 保留 open；待 Store 推送 2.9.5+ 或 GitHub 出现安装资产后执行（watch-wsl295.sh 每日只读监控已挂计划任务）。
- 源码含修复 ≠ 运行时必然修复（理论上仍有剩余缺陷可能），但复现边界与源码边界的完全对齐，已把"版本缺陷区间"从假设升级为双向证据。

## 七、验证命令记录（可复现）

```bash
cd /mnt/c/Users/hh/OneDrive/research/wsl-src-master-sparse
git fetch origin tag 2.9.4 tag 2.9.5 tag 2.9.6 tag 2.9.7 tag 2.7.8 tag 2.7.12
git merge-base --is-ancestor 55d457846d5e5e5d3b82f7c2305480a2aa3893e7 2.9.7   # 0=是
git merge-base --is-ancestor 96b220c785f6ed90e103813cdfc66a723224c657 2.9.5   # 0=是
git diff 2.9.4 2.9.5 -- src/linux/init/GnsPortTracker.cpp
git diff 2.9.5 2.9.7 -- src/linux/init/GnsPortTracker.cpp src/linux/init/localhost.cpp src/linux/init/main.cpp src/linux/inc/seccomp_defs.h
git log 2.9.7..master -- src/linux/init/GnsPortTracker.cpp
```
