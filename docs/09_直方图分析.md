# 33 重启后健康态直方图：200 轮终稿（08-19）

> 日期：2026-08-19 ｜ 类型：只读测量 ｜ 状态：完成（200 轮，13:28 结束）
> 数据：logs/reboot-exclusion/histogram200_dockerON.txt（PID 345659，`-rounds 200 -timeout 30s -wincheck`，11:56 启动）
> 方法：research/analyze_histogram.py（只读解析，不产生 churn，不污染测量）

## 终值统计（200 轮全量）

- **STALL 率 91.5%（183/200）**；OK 8.5%（17/200）。
- OK 时延：min=11ms avg=12.2ms max=26ms（p50=11ms p90=12ms p95=13ms）——注册成功路径极快且稳定。
- win=ok 时延：min=42ms avg=49.9ms max=63ms（n=17）——健康窗口内 Windows 侧转发正常。
- STALL 最长连续串 49 轮，平均串长 16.6 轮 ≈ 每串 **8.3+ 分钟持续窗口**（每轮 30s 超时）。
- 时段分段 STALL 率（每 1/4 轮次）：82% / 92% / 94% / 98%——随时段**单调恶化**。

## 结论

1. **churn 累积退化趋势实锤**：STALL 率随时段单调恶化（82% / 92% / 94% / 98%），200 轮尺度量化支撑 v4 评论 "Churn degrades the path within a single run"——**不是稳态随机，是自增强退化**。
2. **窗口是二元状态**：要么 30s+ 完全不可达，要么 11-26ms 成功，无中间态；窗口持续数分钟（最长串 49 轮 ≥ 24.5 分钟），非秒级抖动。
3. **与端口号无关**：STALL 轮与 OK 轮端口范围重叠、高位占比相近；纯时序窗口。
4. **重启不消除**：整机重启（09:27）后 2h 起测，仍 91.5% STALL——直接支撑"修复方向 = 升级 WSL 2.9.5+"（31 号锚定版本缺陷区间）。
5. **Docker 排除**：与 roundA/B/C（3/8、1/8 成功，Docker ON/OFF 无差异，32 号）对照一致——故障与 Docker Desktop 无关。

## 归档

- 日志：`logs/reboot-exclusion/histogram200_dockerON.txt`（200 轮原始输出）。
- 本文件与 10 号、37/39 号预案已同步终值；#62 关闭、#72 解锁。

## 污染约束（执行期）

- 直方图运行期间禁止任何端口 churn 实验与 WSL/Docker 重启；本分析为只读，未违反。
