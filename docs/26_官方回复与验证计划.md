# 回复官方草稿（microsoft/WSL #41383 → @chemwolf6922）

> 状态：已发送（2026-08-20，评论 id 5351512967）。本文档为发送记录与草稿存档。
> 发送位置：microsoft/WSL issue `#41383` 评论区（评论 id 5351512967）。

## 草稿正文（英文）

Hi @chemwolf6922, thanks for confirming and for the build link.

We will test the dev build with the same port-0 probe used in the report (churn rounds + health-state transient stress) and report the results here. We can also confirm from the official tags that the fix commits (#41051 merge 55d45784, #41125 merge 96b220c7) are already ancestors of 2.9.5, so if the dev build clears the window we will re-verify once 2.9.5+ reaches the Store channel.

Plan: run the probe on the dev build, then follow up with the numbers.
